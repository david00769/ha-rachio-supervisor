# Test notes

The public seed validates syntax and packaging shape first.

The first runtime implementation milestone should add:

- config flow tests
- options flow tests
- coordinator logic tests
- entity naming and state tests
- diagnostics tests
- catch-up decision tests
- moisture write-back tests

